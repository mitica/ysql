SELECT:
  DISTINCT: true
  COLUMNS:
    - COLUMN:
        name: id
    - COLUMN:
        name: d.name
        as: d_name
  FROM:
    SELECT:
      COLUMNS:
        COLUMN:
          name: f.docid
      FROM:
        TABLE:
          name: files
          as: f
      as: d

  LIMIT: 10
  OFFSET: 5